Update all git submodules to their latest commits from remote repositories.

Run:

```sh
git submodule update --remote --merge
```

After updating, show the status of each submodule with their current commit.
