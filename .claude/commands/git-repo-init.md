Initialize all git submodules after cloning the repository.

Run:

```sh
git submodule update --init --recursive
```

After initialization, show the status of each submodule to confirm they are properly initialized.
